// Seed script (run with: npx prisma db seed)
// PRD §11: seed 4 role accounts, 6 vehicles (incl. 1 retired + 1 in-shop),
// 5 drivers (incl. 1 expired-license + 1 suspended).

async function main() {
  // TODO: implement seeding
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
